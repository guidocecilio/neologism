# @TODO PATCH disable the export of user and node. removes the # in the property uri generation, to be compatible with our own rdfcck name convention (to fix).
Index: rdf/contrib/rdf_schema/rdf_schema.module
===================================================================
--- rdf/contrib/rdf_schema/rdf_schema.module	(revision 235)
+++ rdf/contrib/rdf_schema/rdf_schema.module	(revision 236)
@@ -150,7 +150,7 @@
       'table'   => 'users',
       'query'   => 'SELECT uid FROM {users} WHERE uid > 0',
       'uri'     => 'user/%uid',
-      'enabled' => TRUE,
+      'enabled' => FALSE,
     ),
     'vocabulary' => array(
       'title'   => t('Taxonomy vocabulary'),
@@ -176,7 +176,7 @@
       'query'   => 'SELECT nid FROM {node}',
       'uri'     => 'node/%nid',
       'load'    => 'node_load',
-      'enabled' => TRUE,
+      'enabled' => FALSE,
     ),
     'comment' => array(
       'title'   => t('Comment'),
Index: rdf/contrib/rdf_schema/rdf_schema.inc
===================================================================
--- rdf/contrib/rdf_schema/rdf_schema.inc	(revision 235)
+++ rdf/contrib/rdf_schema/rdf_schema.inc	(revision 236)
@@ -22,7 +22,7 @@
 }
 
 function rdf_schema_get_property_uri($class, $property) {
-  return RDF_SCHEMA_URI . $class . '#' . $property;
+  return RDF_SCHEMA_URI . $class . '' . $property;
 }
 
 function rdf_schema_get_entities() {
