Index: modules/node/node.module
===================================================================
--- modules/node/node.module	(revision 271)
+++ modules/node/node.module	(working copy)
@@ -814,7 +814,7 @@
  */
 function node_submit($node) {
   global $user;
-
+var_dump($node);
   // Convert the node to an object, if necessary.
   $node = (object)$node;
 
